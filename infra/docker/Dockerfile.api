FROM python:3.12-slim
WORKDIR /app
COPY . /app
RUN python -m pip install --no-cache-dir -r requirements.txt
ENV FND_HOST=127.0.0.1
CMD ["python", "-m", "uvicorn", "apps.api.app.main:app", "--host", "127.0.0.1", "--port", "8000"]
