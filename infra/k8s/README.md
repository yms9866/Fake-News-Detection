# Kubernetes stubs

These manifests are **not deployable**. They are placeholders (`fnd-api:local`, no probes, no resources, no ConfigMaps).

Do not apply them. When a real environment exists, replace this directory with generated or reviewed production manifests and load CORS, model paths, and secrets from that environment.
