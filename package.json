{
  "name": "fnd",
  "private": true,
  "type": "module",
  "description": "Local-first fake-news analysis platform.",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "python -m uvicorn apps.api.app.main:app --host 127.0.0.1 --port 8000",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "eslint": "^9.12.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.8.0"
  }
}
